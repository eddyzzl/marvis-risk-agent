# Keep the bundled platform runtime compatible with older x86_64-v1 CPUs and
# avoid silently pulling the newest native wheels during installer builds.
# Newer NumPy 2.4+ Windows wheels can require X86_V2 and fail during import on
# older personal computers before MARVIS can show a useful error.
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2
statsmodels==0.14.4
matplotlib==3.9.4
seaborn==0.13.2
pyarrow==16.1.0
duckdb==1.1.3
xgboost==2.0.3
lightgbm==4.3.0
catboost==1.2.7
joblib==1.4.2
