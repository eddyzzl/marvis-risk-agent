# 数据、特征、模型全流程最终代码审查

- 审查日期：2026-07-24
- 固定基线：`16392e473f21282573993ccf2ba57111fb066fac`
- 审查范围：固定基线至提交前工作树的全部数据处理、特征分析、模型开发、Agent、记忆、报告和前端改动
- 最终结论：**CONDITIONAL（实现审查通过；真实材料验收未完成）**

> 2026-07-28 复核修订：原结论只覆盖当时的实现与合成/隔离测试，不能替代真实
> 材料机器预检和责任人签字。下文保留当时的审查证据，但以本次修订后的结论为准。

## Standards 轴

初审发现两项，均已在提交前关闭：

1. 新增功能组件的毛玻璃、圆角和动效与旧版 `DESIGN.md` 的绝对禁用条款冲突。
   已更新设计源，将毛玻璃限定为具有筛选、进度、折叠或钻取功能的工作流组件；
   普通 Agent 文本不得套卡片，同时移除 hover 位移并补齐 reduced-motion 约束。
2. 闭环文档中包含本机绝对路径、真实任务标识和精确业务指标。
   已改为可移植占位、受控审计引用和脱敏机器证据。

复审未发现剩余规范阻断项。

## Spec 轴

数据处理、特征分析和模型开发三条工作流与对应前端规格已实现：

- 多文件识别、预览、角色确认、择键、拼接诊断与可恢复执行。
- Train-only 特征候选/筛选、完整指标、PSI 不可算原因、Agent 推荐、多选分箱及报告。
- 显式 train/test/OOT 设置、特殊值确认、多算法二分类/回归/多分类、单轮调参、
  实验比较、模型报告与逐份安全下载。
- 手动模式使用中央信息流按钮；Agent 模式用自然语言；auto mode 仅在非 HITL
  节点自动继续。
- 右栏只显示父步骤和子任务状态；运行、成功、失败、跳过和等待由图标表达，
  操作入口保留在中央信息流。
- Agent 在拼接、特征、模型结果和异常处生成诊断、建议、恢复方案，并显式引用
  可审计记忆。

在当时已执行的实现测试范围内，未发现可复现的功能缺失、越界扩张或伪实现。
2026-07-28 已另补一条 Agent 模式自然语言 E2E，使用多文件材料完整走通
JOIN → FEATURE → 训练 → 报告 → 交付；它验证受治理的文本到动作链路，但不把
外部大模型的生成质量冒充为确定性验收。

## 对抗与回归验证

- 官方 fast 全量：`4845 passed, 135 deselected`。
- 新工作区闭环 smoke：数据拼接、特征分析、模型开发各 1 条，`3/3 PASS`。
- 独立复审重放：损坏 pickle、伪 XLSX、空壳 PMML 均拒绝；多报告任务隔离、
  六种非 LGB 回归/多分类配方及逐份下载均通过。
- 浏览器回归（1280×720）：
  - 超长上传文件名以省略号展示，弹窗及页面无水平溢出；
  - XGBoost 回归选项映射到连续目标流程；
  - 材料与目标类型不匹配时返回可读诊断；
  - 顶部任务状态栏可从约 172px 收起到约 65px；
  - 页面控制台错误为 0。
- Ruff、格式、JSON、JavaScript 语法和 `git diff --check` 通过。

测试中的 MLP 小样本 `ConvergenceWarning` 是预期的单轮训练告警，不影响流程
和产物验收。

## 不能由代码伪造的外部验收

以下三项仍是正式业务结项的前置条件，不能由代码审查替代：

1. 两个公开信用数据集尚未提供，人工精调 KS 地面真值仍为空；
   `scripts/ks_baseline.py --status` 应保持退出码 `2`，不得以合成数据冒充。
2. 至少一份真实材料仍需业务口径对账与责任人签字；机器预检不能代签。
3. 当前保存的机器报告本身为 `BLOCKED_MACHINE`：A1 预处理链不可追溯，模型
   监控状态为 fail；旧报告中的 A2/A4/A5/B5 `N/A` 也不再被当前脚本接受。
   必须修复实际任务后，连同同材料的 JOIN/Vintage 任务重新生成机器报告。

因此，本次实现可以进入代码审查与 CI，但不能凭这份历史文档宣称正式结项；
只有最新机器预检通过、公开数据基线完成且 B1-B5 外部对账签字齐备后，才能
宣称真实业务验收完成。
